services:
  image-server:
    build: .
    image: ghcr.io/amber-bisht/imageservcies-bun:latest
    container_name: image-server
    restart: always
    ports:
      - "4001:4001"
    volumes:
      - ./data/images:/app/public/images
    environment:
      - PORT=4001
      - IMAGE_SERVER_SECRET=${IMAGE_SERVER_SECRET}
      - BASE_URL=${BASE_URL}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:4001/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
